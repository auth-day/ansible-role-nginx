server {

    listen       80;
    server_name  {{ inventory_hostname }};

    root   /{{ root_dir }}/{{ frontend_home_dir }};
    index  index.html index.htm;

    location / {

            try_files $uri /index.html;
            auth_basic "Authentication required";
            auth_basic_user_file /etc/nginx/.htpasswd;


    }
